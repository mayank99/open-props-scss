<h1>hello</h1>

<p>Try editing <code>index.astro</code> on stackblitz.</p>

<p>For a full list of variables, see the compiled source on <a href='https://unpkg.com/open-props-scss'>unpkg</a>.</p>

<style lang='scss'>
	@use 'open-props-scss' as op;

	:root {
		color-scheme: dark light;

		--page-bg: #{op.$gray-2};
		--page-text: #{op.$gray-9};
		--page-link: #{op.$violet-8};
		--component-bg: #{op.$gray-1};
		--component-border: #{op.$gray-5};

		--font-base: #{op.$font-size-fluid-1} #{op.$font-sans};
		--font-mono: 0.9em #{op.$font-mono};
		--content-width: #{min(95%, op.$size-content-3)};

		@media (op.$OSdark) {
			--page-bg: #{op.$gray-9};
			--page-text: #{op.$gray-1};
			--page-link: #{op.$violet-3};
			--component-bg: #{op.$gray-8};
			--component-border: #{op.$gray-7};
		}
	}

	body {
		background-color: var(--page-bg);
		color: var(--page-text);
		font: var(--font-base);
		max-width: var(--content-width);
		margin-inline: auto;
	}

	code {
		font: var(--font-mono);
		background-color: var(--component-bg);
		padding: op.$size-1 op.$size-2;
		border-radius: op.$radius-2;
		border: 1px solid var(--component-border);
	}

	a {
		color: var(--page-link);
		text-underline-offset: 0.25ex;
	}
</style>
